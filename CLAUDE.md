# allseen.com

## Workflow

- At the end of every implementation, commit and push to `origin main`. Pushing deploys to allseen.com, and changes are reviewed live there.
- Don't run a local build or dev server to verify changes; the live site is the check.
